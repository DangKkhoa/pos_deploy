<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sale Detail</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        h1, h2 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }

        button {
            float: right
        }
    </style>
</head>
<body>
    <%- include('navbar') %>
    <div class="container">
        <h1>Sale Detail</h1>
        <h2>#<%= sale[0].sale_id %></h2>
        <p><strong>Saleperson name: </strong><%= sale[0].fullname %></p>
        <p><strong>Date: </strong><%= sale[0].sale_date.toLocaleDateString() %></p>
        <p><strong>Time: </strong><%= sale[0].sale_time %></p>
        <p><strong>Customer gave: </strong><%= sale[0].amount_given_by_customer %></p>
        <p><strong>Change to customer: </strong><%= sale[0].change_to_customer %></p>
        <table>
            <thead>
                <tr>
                    <th>Product name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                <% sale_details.forEach(detail => { %>
                    <tr>
                        <td><%= detail.product_name %></td>
                        <td><%= detail.quantity %></td>
                        <td><%= detail.unit_price %></td>
                    </tr>
                <% }) %>
            </tbody>
            <tfoot>
                <tr>
                    <td>Total</td>
                    <td><%= sale[0].total_quantity %></td>
                    <td><%= sale[0].total_price %></td>
                </tr>
            </tfoot>
        </table>
        <!-- You can add more sale details here -->
        <a href="/sale_history">Back</a>
        <button>Print</button>
    </div>
</body>
</html>
